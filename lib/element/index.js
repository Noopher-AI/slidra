// Entry point of the <slidra-player> bundle: importing it registers the element.
import { defineSlidraPlayer } from "./slidra-player.js";

export { SlidraPlayerElement, defineSlidraPlayer } from "./slidra-player.js";

defineSlidraPlayer();
