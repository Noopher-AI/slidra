# 05 · 前端交互模式（BDD / Gherkin）
# language: zh-TW

功能: 舞台導航（Figma 式）
  背景:
    假設 我在編輯模式且投影片顯示於舞台

  場景: 以滾輪縮放
    當 我按住 ⌘（或 Ctrl）並滾動滑鼠
    那麼 投影片以游標為中心縮放，範圍 25%–400%
    而且 工具列左側的百分比即時更新

  場景: 以滾輪平移
    當 我不按修飾鍵滾動（含兩指）
    那麼 投影片跟著平移

  場景: 抓取模式
    當 我按下工具列的 ✋ 按鈕
    那麼 目前選取被清除，游標變為手掌
    而且 在投影片、元素或空白處按住拖曳都會平移畫布
    當 我再按一次 ✋
    那麼 回到編輯模式

  場景: 暫時抓取
    當 我按住 Space
    那麼 行為同抓取模式；放開 Space 即恢復

  場景: 縮放選單
    當 我點工具列的百分比
    那麼 從工具列正上方中央長出橫向選單：− 百分比 + ｜ Fit 50% 75% 100% 150% 200% 400%
    而且 目前值以紅字標示
    當 我選 Fit
    那麼 縮放回 100% 並置中

功能: 選取
  場景: 單選
    當 我點一個元素
    那麼 出現紅色選取框、四角把手與左上名稱標籤
    而且 情境列出現在選取框正下方（空間不足則翻到上方）

  場景: 多選
    當 我 ⇧點 另一元素 或 在空白處拖出框選
    那麼 所有命中的元素以虛線群框包住，可整體拖曳，無縮放把手

  場景: 全選 / 取消
    當 我按 ⌘A
    那麼 本頁所有元素被選取
    當 我按 Esc 或點空白處
    那麼 取消選取並關閉所有浮層

功能: 移動與縮放元素
  場景: 拖曳吸附
    假設 我拖曳一個選取中的元素
    那麼 靠近畫面邊／中線／其他元素邊或中心時吸附，並顯示粉紅輔助線
    而且 按住 ⌥ 時不吸附
    當 我放開滑鼠
    那麼 這次移動成為一筆可 Undo 的歷史

  場景: 縮放
    當 我拖曳四角把手
    那麼 元素依把手方向縮放，最小 3cqw × 0.6cqh，不超出投影片

功能: 就地編輯
  場景: 文字
    當 我雙擊文字（或選取後按 Enter）
    那麼 文字變為輸入框；Enter 提交、Esc 取消、失焦提交

  場景: 表格儲存格
    當 我點儲存格
    那麼 該格被選取（⇧點延伸範圍），右欄 Style › Object 顯示 Cell 區塊
    當 我雙擊儲存格
    那麼 進入編輯；Tab 跳下一格
    當 我在儲存格上按右鍵
    那麼 出現 Edit / Bold / 插列插欄 / 合併或取消合併 / 刪列刪欄

  場景: 欄寬
    當 我拖曳表格第一列的欄界
    那麼 該欄寬度即時改變，放開後入歷史

功能: 底部玻璃工具列
  場景: 插入（所有類型先詢問）
    當 我按 Text / Image / Video / Audio / Table / Chart
    那麼 從工具列正上方中央長出對應面板
    而且 Text：輸入 + 樣式預設 + 對齊，Enter 直接插入
    而且 Image/Video/Audio：拖放或選檔、URL、caption；留空插入占位
    而且 Table：8×6 格 hover 預覽、click 鎖定尺寸；主題；表頭
    而且 Chart：類型、系列數、類別數、調色盤
    當 我按 Insert
    那麼 元素加到目前頁並被選取；面板關閉

  場景: Shape / Arrange 選單
    當 我按 Shape
    那麼 長出橫向選單 Rectangle / Ellipse / Line
    當 我按 Arrange
    那麼 長出三欄選單 Align / Distribute / Order；無選取時項目停用；Distribute 需 ≥3

  場景: 停用態
    假設 沒有任何選取
    那麼 Animate / Arrange 半透明不可按；Group 亦不可按（需 ≥2 或整組）

功能: 群組（含巢狀）
  場景: 成組
    假設 我選取了 2 個以上元素
    當 我按 Group
    那麼 它們成為一組，成員原有的物件動畫被移除（toast 提示）
  場景: 選取群組
    當 我點群組中任一成員
    那麼 整組被選取（實線框），標籤顯示群組名
  場景: 鑽入
    當 我在已選取的群組成員上雙擊
    那麼 選取縮到下一層（子群組或該元素），標籤顯示路徑「Group 2 › Group 1」
  場景: 巢狀
    假設 選取包含一個已成組的群組與其他元素
    當 我按 Group
    那麼 外層再包一層
  場景: 解組
    假設 整組被選取
    當 我按 Ungroup
    那麼 只解開目前鑽到的那一層，該群組動畫被移除

功能: 物件動畫（PPTX 心智）
  場景: 新增
    假設 有選取
    當 我按工具列 Animate
    那麼 長出面板：效果卡（循環預覽）、Start（On click / With / After）、Duration
    當 我按 Add animation
    那麼 動畫加到所選元素；右欄切到 Animate › Object；卡片附在清單末端
  場景: 群組動畫
    假設 整組被選取並新增動畫
    那麼 成員共用效果；第一個帶觸發時機、其餘 With previous；清單只顯示一張「Group N (n)」卡
  場景: 排序與參數
    當 我在卡片按 ↑↓
    那麼 點擊順序改變；舞台上編號徽章同步
    當 我改效果 / Start / Duration / Delay
    那麼 立即套用並入歷史
  場景: 預覽
    當 我按 Preview
    那麼 依步序播放整頁動畫；單卡 ▶ 只播該段

功能: 頁面動畫
  場景: 設定
    當 我在 Animate › Page 選 Enter/Exit 效果與時長
    那麼 播放進入該頁時播 Enter；離開前先播 Exit 再切頁
    當 我按 Apply to all slides
    那麼 所有頁套用相同設定

功能: 播放
  場景: 前進
    假設 在播放模式
    當 我按 → / Space / 點畫面
    那麼 若尚有動畫步則揭露下一步，否則到下一頁
  場景: 退出
    當 我按 Esc 或 Exit
    那麼 回到編輯模式

功能: 與 AI 協作
  場景: 對元素留言
    假設 選取了一個元素
    當 我按情境列的 Comment to AI
    那麼 從選框長出玻璃留言框；⌘↵ 或 Add comment 儲存
    那麼 選框標籤旁出現紅色編號；對話欄 Pinned context 多一列（頁碼 + 截斷留言）
  場景: 對整頁留言
    當 我按縮圖右上的留言鈕
    那麼 同上，目標為整頁
  場景: 跳轉與編輯
    當 我點 Pinned context 的一列 或 舞台上的編號
    那麼 跳到該頁、選取該元素、打開留言框顯示原文；可修改或 Delete
  場景: 送出
    當 我送出訊息
    那麼 訊息連同所有 pins 一起作為 context 送給 agent（輸入列顯示 n pinned）
  場景: Agent 編輯中
    假設 收到 lock 事件
    那麼 標題列顯示「Agent editing · undo paused」，Undo/Redo 停用

功能: 頁面管理
  場景: New / Templates
    當 我按左欄 New
    那麼 展開版面清單（Blank + 範本 + From outline…）
  場景: 從大綱生成
    當 我貼大綱並按 Draft with agent
    那麼 出現 Running 指令卡；1.4s 後新頁插入目前頁之後（縮排行為副標）
  場景: 拖曳排序
    當 我拖縮圖到另一位置
    那麼 紅色插入線提示；放開後重排並入歷史

功能: 歷史
  場景: Undo / Redo
    當 我按 ⌘Z / ⇧⌘Z 或標題列按鈕
    那麼 回退／重做上一筆結構性變更（移動、縮放、文字、表格、圖表、排序、新增刪除、動畫、群組、頁面尺寸不含）
