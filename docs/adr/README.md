# 架構決策紀錄

一份 ADR 記錄一個難以反轉、脫離脈絡會讓人困惑、且來自真實權衡的決定。**ADR 不會被刪除或改寫**——被推翻的決定留在原地，因為它記錄的是當初的判斷，那本身就是有價值的資訊。

## 檔名慣例

| 檔名 | 意思 |
| --- | --- |
| `NNNN-slug.md` | 完全成立 |
| `NNNN-slug.amended.md` | **有條款已失效**，檔案開頭的橫幅列出哪些死了、由哪一份取代、哪些仍然成立 |

被修訂的 ADR 一律做兩件事：檔名加上 `.amended`，並在標題下方加一則橫幅。細節的修訂另外以引言註記留在原文對應的位置。

引用 ADR 一律用編號（`ADR-0002`），不要用檔名——檔名會因為修訂而變。

## 目前的清單

| | 決定 | 狀態 |
| --- | --- | --- |
| 0001 | SVG 是本體 | 部分失效 → 0016 |
| 0002 | CLI 是唯一的操作語彙 | 部分失效 → 0014 |
| 0003 | `.comot` 容器 | 部分失效 → 0016 |
| 0004 | 簡報內容對 agent 唯讀 | 部分失效 → 0015 |
| 0005 | 步驟驅動的動畫 | 大幅修訂 → 0007、0009 |
| 0006 | 以 ACP 接上外部 agent | |
| 0007 | 播放 `.comot` 需要 CoMotion | |
| 0008 | 一張投影片自成一體 | |
| 0009 | 動態是一份有序的效果清單 | |
| 0010 | 投影片內容不可信 | 部分失效 → `#29` |
| 0011 | 檢視模式跑一支命中回報 script | |
| 0012 | 每個元素包在一層 `<g transform>` 裡 | |
| 0013 | 用範本與鎖定取代母片 | |
| 0014 | 樣式命令用 SVG 屬性名，走白名單 | |
| 0015 | agent 可以匯入資產 | |
| 0016 | 字型隨 `.comot` 打包，單獨開啟的投影片降級成系統字型 | |
