__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
build/
dist/

# Fleet plan records — local working notes, not part of the product.
.fleet/

# macOS
.DS_Store

# Dependencies and build caches
node_modules/
.next/
*.tsbuildinfo

# Rust build artifacts (Cargo.lock is committed; the crate is a binary crate)
/target/

# Local quick-start scratch
.quickstart/

# E2E screenshot-comparison failure diagnostics (actual + diff PNGs)
e2e/__screenshots__/__failed__/

# Sandbox QA layer scratch (screenshots, iframe-session cache) — debugging
# aids, not part of the product or its test fixtures.
qa/out/
.vercel
.env*
